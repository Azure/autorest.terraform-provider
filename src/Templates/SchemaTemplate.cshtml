@using AutoRest.Terraform
@using AutoRest.Terraform.Templates
@inherits TfProviderTemplateBase<SchemaGenerator>
func @(Model.FunctionName)() *@(ReferencePackage("schema")).Resource {
@{
    Indentation.Indent();

@:@(Indentation)return &@(ReferencePackage("schema")).Resource {

    Indentation.Indent();

@:@(Indentation)Create: @(OtherModel<CreateGenerator>().FunctionName),
@:@(Indentation)Read:   @(OtherModel<ReadGenerator>().FunctionName),
@:@(Indentation)Update: @(OtherModel<UpdateGenerator>().FunctionName),
@:@(Indentation)Delete: @(OtherModel<DeleteGenerator>().FunctionName),
@:@(Include<SchemaFieldsSubTemplate, TfProviderField>(Model.RootField))

    Indentation.Outdent();

@:@(Indentation)}

    Indentation.Outdent();
}
}
@EmptyLine